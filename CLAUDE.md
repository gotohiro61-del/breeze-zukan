# CLAUDE.md — BREEZE（体験ずかん）運用ルール（AI長時間運用対応）

作成日：2026-07-30
目的：Claude Codeが本リポジトリで安全に長時間自律作業するためのルールを定義する（エバースグループ共通ルール準拠）。

---

## 1. リポジトリの目的

- 沖縄・家族特化の海辺キャンプ場「海辺の王国」の全アクティビティを、写真カード→詳細表示で見せる**体験ずかん（単一ページのプロトタイプ）**
- 担当者とのイメージ共有用。GitHub Pagesで公開中（クライアントサイドの簡易パスワードゲート付き）
- リポジトリ名はBREEZEだが、GitHub上のリポジトリ名は `breeze-zukan`（不一致に注意）

## 2. 技術構成

| 項目 | 内容 |
|------|------|
| 言語 | HTML / CSS / Vanilla JavaScript（依存ゼロ） |
| フレームワーク | なし。`index.html`（約900行）にCSS・JS・データを全部内包 |
| パッケージ管理 | なし（package.json・lockfileなし） |
| ビルド補助 | Python 3（標準ライブラリのみ） |
| テスト | なし（ブラウザでの手動確認のみ） |

## 3. 実行・ビルド方法

- **プレビュー**: `python -m http.server 4173` でローカル確認（`.claude/launch.json` 定義済み）
- **配布ビルド**: `python build_standalone.py` → 画像をdata URI化した単体ファイル `BREEZE-zukan.html`（約7.9MB）を再生成
- コンテンツの追加・変更は `index.html` 内の `ITEMS` 配列を編集する（39画像／28体験）

## 4. ディレクトリ構成と編集区分

| 対象 | 区分 | 備考 |
|------|------|------|
| index.html | 編集可（本体） | CSS+JS+ITEMSデータ+パスワードゲート全部入り |
| images/ | 原則読み取り専用 | 差し替えは同名上書き運用。削除・リネームは `ITEMS` の参照が壊れるため禁止 |
| build_standalone.py | 明示指示時のみ編集 | 下記の地雷参照 |
| BREEZE-zukan.html | **コミット禁止**（gitignore済） | `git add -f` 厳禁。ゲート無し版のため公開すると素通りになる |
| cloudflared.exe | **コミット禁止**（gitignore済、54MB） | |
| .claude/ | 編集禁止 | ローカル設定 |

## 5. プロジェクト固有の地雷（必読）

1. **mainへのpush＝即・本番公開**。GitHub Pagesがmainブランチ直配信のため、pushした瞬間に世界に公開される
2. `build_standalone.py` は `index.html` 内の**3つのアンカー文字列に assert で完全一致依存**している：
   - `url('images/hero.webp')`
   - `const BOOK_URL = "https://breeze-camp.com/";`
   - 特定の `<img src=...>` タグ全文
   この3箇所を書き換えるとビルドが即エラーで落ちる。触る場合はビルドスクリプト側も同時に直す
3. パスワードゲートはクライアントサイドの平文実装（**真の認証ではない**）。パスワード値をレポート・ログ・コミットメッセージに書き出さない
4. 外部通信は予約リンク `https://breeze-camp.com/` のみ。API・DB・アナリティクス等の外部送信を新規追加しない

## 6. 機密情報・個人情報

- APIキー・トークン・パスワード類の内容を表示・保存・コミットしない
- ゲートのパスワード値を文書やレポートに転記しない
- 実在個人の写真・氏名の扱いはユーザー指示に従う

## 7. Git運用ルール（エバース共通）

- **mainブランチ上で自動作業しない**。長時間のAI作業は専用ブランチ（ai-automation-setup 等）で行う
- 自動作業中のmainへの直接コミット・push・mergeは禁止
- **force push禁止・rebase禁止**（履歴の書き換え禁止）
- `git reset --hard`・`git clean -fd` 等の破壊的Git操作は禁止
- `git add .`・`git add -A` は禁止。**コミット対象ファイルは個別に指定する**
- 作業開始前と終了時に `git status` を確認する
- 未コミット変更がある場合、既存変更と今回の変更を混ぜない
- **pushは明示的な指示がある場合のみ実行する**（自動push禁止。特に本リポジトリはpush＝本番公開）
- 別セッション・別PCからの変更が疑われる場合は、勝手にpull・merge・rebaseせず停止する

## 8. 長時間自動実行時のルール（エバース共通）

- 初回は**最大30分・最大3サイクル**まで（実績を確認してから段階的に拡大）
- 同じエラー・同じ修正を3回繰り返したら停止する
- 成功条件を実行前に明文化する（機械的に判定できる形で）
- 毎サイクル終了時に状態を保存し、途中再開可能にする
- 対象外ファイルへの変更を検知したら停止する
- 本番環境・外部サービスへの接続・送信は行わない
- 自動コミットは専用ブランチ上で明示的に許可された場合のみ。**自動pushは行わない**

## 9. 品質チェック（作業後に確認）

- [ ] ブラウザ（http.server プレビュー）で表示崩れ・JSエラーがない
- [ ] `ITEMS` 配列と `images/` の参照が一致している
- [ ] `build_standalone.py` のアンカー3箇所を壊していない（変更時は実行して確認）
- [ ] BREEZE-zukan.html / cloudflared.exe をステージングしていない
- [ ] `git diff` に意図しない変更がない

## 10. 停止条件

次の場合は作業を停止し、ファイルを変更せず報告する：
mainブランチ上にいる（自動作業時）／対象範囲が不明確／push・本番公開が必要になった／破壊的Git操作が必要になった／同一エラーが3回続いた／指定時間・サイクル数に達した

## 11. 作業完了時の報告（エバース共通）

1. 実施した作業 2. 作成・変更したファイル 3. 変更理由 4. 実行した確認 5. 検出したリスク 6. 未解決事項 7. git status 8. コミットの有無 9. pushの有無 10. 次に推奨する行動

## 12. 改修・レビュー対応の進め方（全プロジェクト共通・2026-09-20追加）

上記の既存章（Git運用・安全ルール・停止条件・報告）はすべて有効で、本章はその上で動く。矛盾がある場合は本章の下にある「既存ルールとの関係」に従い、判断に迷う場合は停止して確認する。

指摘・レビュー・改修依頼を受けたときは、次の型で進める。

1. **まず検証**: 指摘を実コードで全件確かめ、「事実か／このシステムでの重要度／直す手間」を表で示す。憶測で肯定も否定もしない
2. **着手前に計画を表で出す**: 手順（実行順）・直し方・担当モデル・依頼後の所要時間（手順ごと＋合計）・後藤さんがやること。後藤さんの手が必要な地点だけで区切り、「進めて」を言う回数を明記する。時間は見積もりであり保証ではない
3. **「進めて」の後はノンストップ**: 返事を待たずに最後まで進める。止まるのは判断が必要なときだけで、その項目は保留にして他を進め、最後にまとめて聞く
4. **モデルの使い分け**（作業役＝サブエージェントのモデルを指定する）:
   - 指揮・最終確認・ブラウザ実機確認・アップ・報告 = この会話のモデル
   - 難所（認証・DB制約やトランザクション・お金の計算・外部連携・レビュー）= Opus の作業役
   - 単純作業（依存更新・文言・README・数行の修正）= Sonnet の作業役
   - 実装した作業役に自分の仕事を承認させない。レビューは別の Opus に依頼し、修正後に同じレビュー役へ再確認させる
5. **便（バッチ）ごとに必ず**: 実装 → Opus レビュー → 反映 → レビュー役の再確認 → 型チェック・テスト・実DBテスト・ビルド → ブラウザ実機確認 → アップ（GitHub → 本番）。検証に失敗した変更はその便から外して保留し、独立した変更だけ反映する。DBの制約・巻き戻し・移行は本番と別のテスト用DBで実際に確認する
6. **進捗は毎回「終わった／残り」の表（モデル列つき）で出す**。完了報告には検証結果（テスト件数・ビルド・レビュー・本番反映）・実機で未確認のもの・持ち越しを明記する。後藤さんの作業が要るもの（GAS 貼り直し等）は「適用待ち」と「稼働中」を分けて書き、可能なら Chrome 経由で自分で済ませる
7. **記録**: 計画書を docs/ に置き、便ごとの実施記録・持ち越し・実績時間を追記してからアップする

**数行以内の修正**: 数行以内の修正はレビュー役（手順4・5の別の Opus によるレビューと再確認）を省略してよい。ただし本番反映・お金の計算・認証・DB・外部連携に触れる修正は行数に関わらず省略しない。省略したときは完了報告にその旨を書く。

**既存ルールとの関係（優先順位）**:
- **「進めて」＝明示的な指示**: 手順2の計画表に「アップ」「ビルド」「実DBテスト」などの工程と、その影響（本番反映・DBスキーマ変更・課金・デプロイ先）を明記し、その計画に対して後藤さんが「進めて」と言ったとき、それを既存章の「明示的な指示」「ユーザーの明示許可」とみなす（計画表に明記した工程についてのみ。機密情報・個人情報・.env・CI設定など本章が扱わない領域の「明示的な指示」には及ばない）。計画表に書いていないアップ・本番反映・DB変更・課金・デプロイは、既存章どおり止まって確認する
- **外部への送信・投稿は対象外**: メール送信・チャット送信・SNSや社内ツールへの投稿・予約投稿は、本章の「ノンストップ」に含めない。既存章どおり1件ごとに許可を得る。手順6の「Chrome 経由で自分で済ませる」は、閲覧・確認・貼り付けなど送信を伴わない作業に限る
- **AIが実行しない操作**: 既存章が「AIセッションから実行しない」「Claude が自動実行しない」と定めている操作（デプロイスクリプト・本番DBに直接触るスクリプト・実IMAP接続・課金を伴う実行・破壊的スクリプト 等）は、計画表に書いて「進めて」を得ても AI は実行しない。手順2の「後藤さんがやること」に入れ、手順6の「適用待ち」として報告する。例外は手順6が明示する GAS の貼り直し（clasp push を含む）で、計画表に「本番反映・影響する利用者・戻し方」を明記して「進めて」を得て、手順5の検証を通った後にだけ行う。それ以外の本番コード・本番設定の貼り付けと保存は「貼り付け」に含めない。GAS の貼り直しの前に、既存章が定める push 対象の確認（`.claspignore` と送信されるファイル一覧）を必ず行い、貼り直し後は既存章の記録ルール（デプロイ番号と変更内容の対応＝HISTORY.md 等）に追記する。この条件をすべて満たした貼り直しに限り、既存章の品質チェックの「clasp push をしていない」と停止条件の「本番反映（clasp push）が必要になった」は適用しない（新規デプロイ番号の発行＝clasp deploy は例外に含めず、手順2の「後藤さんがやること」に入れる）
- **実機確認の範囲**: 手順5のブラウザ実機確認は、既存章が実データでの実行を禁じている場合（「実データでボタン挙動を実行して検証しない」「検証はモック経路で行う」等）、画面表示・DOM・コードの確認とモック経路での検証に留める
- **停止条件は生きている**: 既存章の停止条件（同一エラー3回・破壊的Git操作・計画に無い本番送信が必要になった・機密情報や個人情報を扱う可能性 等）は本章でも有効。既存章の「初回は最大30分・最大3サイクル」は無人の自動実行向けの上限であり、後藤さんが計画表を見て「進めて」と言った作業は、その計画の所要時間の範囲で最後まで進める（超えそうなら進捗表を出して続ける）
- **同じ主題の既存章・スキルがある場合**: 本章を全社共通の型として使い、既存章（例: 「指摘対応ルール」）や専用スキルは各手順（検証・レビュー・反映）の具体的なやり方として引き続き使う。既存の完了報告の形式（10項目＋「要するに」等）がある場合は、手順6の内容をそれに加える
- 上記で決まらない矛盾は、より安全な方を優先する

**このリポジトリに無い工程の扱い**: 手順5のうちこのリポジトリに存在しない工程（型チェック・テスト・実DBテスト・ビルド・ブラウザ実機確認）は省略し、代わりに文書の整合性確認（リンク切れ・数値・日付・既存記述との矛盾）に置き換える。手順7の置き場は、docs/ が無ければ既存の文書フォルダの慣習に合わせ、文書フォルダ自体が無ければ docs/ を新設する。省略・読み替えた工程は完了報告に明記する。
